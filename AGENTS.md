# LinLi Tool (鄰里工具) - 專案工程與架構開發規範

本專案為社區鄰里工具共享與維護平台，採用 FastAPI (後端) + React/TypeScript (前端) + Gemini Vision AI。為確保系統安全、合規性與比對精準度，所有開發與功能擴充必須嚴格遵循以下守則：

---

## 一、Vision AI 雙階段驗收門禁法則 (Two-Stage Inspection Gate)

所有涉及工具影像比對之流程（上架 D1、現場取件 Check-in、歸還驗收 Check-out），均必須遵守**兩階段防禦門禁**：

1. **第一道門禁：輸入有效性與同實體檢核 (Validity & Same-Entity Check)**
   - **非關物品立即攔截 (INVALID_OBJECT)**：凡上傳生活雜物（馬克杯、文具、餐具等非工程工具），必須在第一時間阻斷，禁止進入差分計算，後端拋出 HTTP 422，前端鎖定訂單與結算按鈕。
   - **同類跨品牌調包攔截 (TOOL_SWAP_DETECTED)**：借出與歸還/取件即使同屬相同品類（如皆為電鑽），若品牌銘牌或外觀不符（例如原借出 Bosch，歸還牧田 Makita），必須精準判定為調包並即刻阻斷。
2. **第二道門禁：差分損壞與責任計算 (Differential Inspection)**
   - 僅在通過第一道門禁確認為「同一實體物件」後，方可對比磨損程度（MATCH 正常損耗全退、MINOR_DIFF 正常磨損、DAMAGE_DETECTED 結構損壞）。

---

## 二、前後端驗收真實直通與 Fail-Closed 防呆原則

1. **後端直通優先 (Backend-First API)**：
   - 涉及身分存證、同物件核對（Check-in）或損壞結算（Check-out）之操作，前端必須呼叫後端專屬端點（如 /api/v1/items/verify-same-object、/api/v1/orders/{id}/check-out），直通 AI Gateway 進行多模態比對。
2. **防呆機制一律採 Fail-Closed**：
   - 前端或後端 Mock 降級邏輯嚴禁採取「只要不是特定錯誤就預設放行」的設計。
   - 對於任何未知照片、非修繕物品特徵，一律預設為 `requires_retake=True` 與 `is_same_object=False`。
   - 比對未通過時，前端必須清空存證雜湊（SHA-256），並維持在未取件/核銷狀態，絕對禁止推進訂單至 IN_USE。

---

## 三、45 度角指引與 Vision Token 成本最佳化 (258 Tokens)

1. **拍照角度規範**：
   - 系統各階段引導框與重拍指引中，必須明確提示借用人：**「45 度側身特寫，露出品牌 LOGO 與夾頭銘牌」**。
   - 效益：一張相片同時涵蓋防調包銘牌與功能結構，免除多視角補拍。
2. **Token 鎖定防線**：
   - 前端在傳送相片前必須透過 Canvas 進行邊緣等比壓縮（最長邊 768px、JPEG 85%），將 Gemini 多模態呼叫嚴格鎖定在 1 個 Tile (258 Tokens，節省 93% 頻寬與費用)。
   - 後端對極度模糊、全黑或空白照片必須以 Pillow 進行 Tier 1 本機 0-Token 秒判攔截。

---

## 四、主管機關金融微文案合規原則 (Zero Forbidden Words)

依據金管會與消保法規標準，本互助平台嚴禁出現保險法特許名詞：
- ❌ **絕對嚴禁詞彙**：「保險」、「保費」、「理賠」
- ✅ **法定合規專有名詞**：
  - 「保險」➔ **「互助保障池」** 或 **「社區互助保障」**
  - 「保費」➔ **「互助保障金」** 或 **「維護費」**
  - 「理賠」➔ **「責任補貼」** 或 **「損害補償」**
- 所有 UI 畫面、API 回傳說明、RAG 知識庫與提示詞，均須落實 0 次違規詞檢核。
